import { homedir } from 'os';
import { 
  chdir,
  argv, cwd,
  stdin, stdout,
  exit, nextTick,
  } from 'process';
import readline from 'readline';

export {
  readline,
  homedir,
  chdir,
  cwd,
  exit,
  nextTick,
  stdin,
  stdout,
  argv,
}