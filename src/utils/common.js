import { homedir } from 'os';
import { 
  chdir,
  argv, cwd,
  stdin, stdout
} from 'process';
import readline from 'readline';

export {
  readline,
  homedir,
  chdir,
  cwd,
  stdin,
  stdout,
  argv,
}